#TimerMap

WeakMap with timer

## example
```javascript
// Create: default timer is "10seconds"
var timerMap = new TimerMap(500)

// Property
timerMap.object

// Method
timerMap.set("a", 10)
timerMap.get("a")
````